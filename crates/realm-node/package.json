{
  "name": "@realm/realm-node",
  "version": "0.1.0",
  "description": "Node.js SDK for Realm.ai with host-side model storage",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "neon build --release",
    "install": "neon build --release",
    "test": "node test.js"
  },
  "keywords": [
    "llm",
    "wasm",
    "inference",
    "ai",
    "realm",
    "nodejs"
  ],
  "license": "MIT OR Apache-2.0",
  "devDependencies": {
    "neon-cli": "^0.11.0",
    "@types/node": "^20.0.0"
  }
}
